---
layout: base
title: Events
---

<h2 class="section-link">{{title}}</h2>
<main class="post-list events-list">
    {% for post in collections.events %}
    {% include event-card with post %}
    {% endfor %}
</main>